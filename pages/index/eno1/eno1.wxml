<view>
  <view class="tt">
    <text>请根据您的需求选择</text>
  </view>
  <block wx:for="{{menuKey}}" wx:for-item="item">
    <view class="index" catchtap="toSpecies" data-menu-id="{{item.id}}">
      <image src="{{item.icon}}"></image>
      <text>{{item.name}}</text>
    </view>
  </block>
</view>